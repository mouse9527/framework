dependencies {
    api(project(":jwt-core"))
    optional("com.fasterxml.jackson.core:jackson-databind:2.12.2")

    optional("org.springframework.boot:spring-boot-starter:$springBootVersion")

    testImplementation(project(":test"))
}
